$first_name = $srcObj["personal_first_name"]
$last_name = $srcObj["personal_last_name"]
$sz_id = $srcObj["profile_uid_ne_attribute"]

$display_name = "$($first_name) $($last_name) ($($sz_id))"
$display_name


